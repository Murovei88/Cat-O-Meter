route:
  receiver: default
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default
    # Пример: для Telegram/Email добавьте интеграции ниже
    # telegram_configs:
    #   - bot_token: "${TELEGRAM_BOT_TOKEN}"
    #     chat_id: "${TELEGRAM_CHAT_ID}"
    # email_configs:
    #   - to: "ops@example.com"
    #     from: "alertmanager@example.com"
    #     smarthost: "smtp.example.com:587"

